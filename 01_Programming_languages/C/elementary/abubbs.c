#include<stdio.h>
int main()
{
    int i,j,n,temp,a[20];
    /*************************************************************************************/
    printf("ent no. of elements\n");
    scanf("%d",&n);

    /*************************************************************************************/
    printf("\nenter elements");
    for ( i = 0; i < n; i++)
    {
        scanf("%d" , &a[i]);
    }

    /*************************************************************************************/
    for ( i = 0; i < n; i++)
    {
        printf("%d\t" , a[i]);
    }
    printf("\n");
    /*************************************************************************************/
    int issorted=0;
    int k=1;
    
    for ( i = 0; i < n-1; i++)         // no. of passes
    {
        issorted = 1;
        for ( j = 0; j < n-i-1; j++)  // no. of comparisions
        {
            if(a[j+1]<a[j]){
                temp = a[j+1];
                a[j+1] = a[j];
                a[j] = temp;
                issorted = 0;
            }
        }
        
        if (issorted==1)
        {
            break;
        } 
        
    }
    
    /*************************************************************************************/
    printf("\n array after sorting\n");
    for ( i = 0; i < n; i++)
    {
        printf("%d\t" , a[i]);
    }

    /*************************************************************************************/

    return 0 ;
}