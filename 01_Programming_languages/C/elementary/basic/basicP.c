#include<stdio.h>
#include<math.h>
int main()

{  
    //printf("my name is amruta\nmy hobbies are to do coding and trouble my sis sometimes");

    //float a;
    //printf("enter your fav. no:");
    //scanf("%f",&a);
    //printf("your fav. no is %f",a);

    /*
    int a , b;
    printf("enter your birth year:");
    scanf("%d",&a);
    b = 2021 - a ;
    printf("your age is %d !!",b);
    */

    /*
    float a , b , c , avg ;
    printf("enter your marks in 3 subjects:");
    scanf("%f %f %f",&a,&b,&c);
    avg =(a+b+c)/3;
    printf("your average in 3 sub. are: %f",avg);
    */

   //int a , b ;
   /*
   int a , b , temp ;
   printf("enter 2 numbers you want to swap:");
   scanf("%d %d",&a,&b);
   */
   /*
   a = a+b;
   b = a-b;
   a = a-b;
   */
   /*
   temp = a ;
   a = b ;
   b = temp ;
   */
   //printf("value of a after swaping is %d and that of b is %d ",a,b);

   /*
   double a , b ;
   printf("enter the number whose square root you want to find:");
   scanf("%lf",&a);
   b = sqrt(a);
   printf("square root of the number is %lf", b);
   */

   /*
   int l,b , area ;
   printf("enter l and b :");
   scanf("%d %d",&l,&b);
   area = l*b;
   printf("%d",area);
   */
    

    return 0;
}