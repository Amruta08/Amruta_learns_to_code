#include<stdio.h>
int main()
{
    int i , n,num,pos,val,arr[20]  ;
    int found = 0 ;
    /*************************************************************************************/
    printf("ent no. of elements\n");
    scanf("%d",&n);

    /*************************************************************************************/
    printf("enter elements");
    for ( i = 0; i < n; i++)
    {
        scanf("%d" , &arr[i]);
    }
    
    /*************************************************************************************/
    printf("enter no. to be searched\n");
    scanf("%d",&num);
    
    /*************************************************************************************/
    for ( i = 0; i < n; i++)
    {
        if (arr[i]==num)
        {
           found = 1;
           pos = i ;
           printf("%d is found at %d\n" , num , pos);
           break;
        }  
    }
    
    if (found==0)
    {
        printf("\nnot found :(");
    }
    
    /*************************************************************************************/
    

    return 0;
}