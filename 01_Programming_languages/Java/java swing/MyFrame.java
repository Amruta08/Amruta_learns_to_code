import javax.swing.ImageIcon;
import javax.swing.JFrame;
import java.awt.Color;

public class MyFrame extends JFrame {

    MyFrame(){

        // this.setVisible(true);    // make this visible
        // this.setTitle("Title");   // sets title of this
        // this.setSize(420,420);    // sets x-dimension and y-dimension of this
        // this.setResizable(false); // prevent's this from being resized

        // //this.getContentPane().setBackground(Color.PINK);   // change color of background , but first import java.awt.Color
        // //this.getContentPane().setBackground(new Color(123,50,250)); // rgb color value
        // this.getContentPane().setBackground(new Color(0xFFF346));  // hex value


        // //this.setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE); // exit out of the application
        // //this.setDefaultCloseOperation(JFrame.HIDE_ON_CLOSE); // Default value 
        // //this.setDefaultCloseOperation(JFrame.DO_NOTHING_ON_CLOSE);  // does nothing on click
        // this.setDefaultCloseOperation(JFrame.DISPOSE_ON_CLOSE);
        // ImageIcon image = new ImageIcon("logo.png"); // create an ImageIcon
        // this.setIconImage(image.getImage());        // Change icon of this

        // new MyFrame();
   
    }
    
}
